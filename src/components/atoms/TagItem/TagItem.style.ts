import { StyleSheet } from 'react-native'

export const styles = () =>
	StyleSheet.create({
		tagConatiner: {
			borderWidth: 1,
			borderRadius: 10,
			padding: 10,
			flexDirection: 'row',
			justifyContent: 'space-between',
			alignItems: 'center',
		},
	})
