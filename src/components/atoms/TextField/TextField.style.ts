import { StyleSheet } from 'react-native'

export const styles = () =>
	StyleSheet.create({
		textField: {
			borderWidth: 1,
			borderRadius: 10,
			paddingHorizontal: 12,
			paddingVertical: 4,
		},
	})
